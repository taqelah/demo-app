name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version tag (e.g. v1.0.0)'
        required: true

permissions:
  contents: write

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test

      - name: Build debug APK
        run: flutter build apk --debug --no-tree-shake-icons

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: build/app/outputs/flutter-apk/app-debug.apk

  build-ios:
    name: Build iOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      # Only Podfile.lock + Runner/Info.plist are tracked for iOS, so regenerate the
      # rest of the Xcode project. flutter create leaves the tracked Info.plist in place.
      - name: Regenerate iOS project
        run: flutter create --platforms=ios .

      - name: Verify deeplink scheme present
        run: plutil -p ios/Runner/Info.plist | grep -q demoapp

      - name: Build iOS Simulator app (debug)
        run: flutter build ios --debug --simulator

      - name: Create .app zip
        run: |
          cd build/ios/iphonesimulator
          zip -r Runner.app.zip Runner.app

      - name: Upload iOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: build/ios/iphonesimulator/Runner.app.zip

  release:
    name: Create GitHub Release
    needs: [build-android, build-ios]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: android-apk
          path: artifacts/android

      - name: Download iOS App
        uses: actions/download-artifact@v4
        with:
          name: ios-app
          path: artifacts/ios

      - name: Rename artifacts
        run: |
          mv artifacts/android/app-debug.apk artifacts/DemoApp-${{ github.event.inputs.version }}-debug.apk
          mv artifacts/ios/Runner.app.zip artifacts/DemoApp-${{ github.event.inputs.version }}-debug-ios.app.zip

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          generate_release_notes: true
          prerelease: true
          files: |
            artifacts/DemoApp-${{ github.event.inputs.version }}-debug.apk
            artifacts/DemoApp-${{ github.event.inputs.version }}-debug-ios.app.zip
